import React from 'react'

const Recommend = (props) => {
    
    if (!props.show || !props.token) {
      return null
    }
  
    return (
      <div>
      </div>
    )
  }

export default Recommend